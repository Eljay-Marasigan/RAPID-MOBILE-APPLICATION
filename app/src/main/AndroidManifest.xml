<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.RAPIDOSM"
        tools:targetApi="31">
        <activity
            android:name=".TermsAndConditionsActivity"
            android:exported="false" />
        <activity
            android:name=".ChatActivity"
            android:exported="false" />
        <activity
            android:name=".UserChat"
            android:exported="false" />
        <activity
            android:name=".GlobalChat"
            android:exported="false" />
        <activity
            android:name=".FAQ"
            android:exported="false" />
        <activity
            android:name=".AboutRapid"
            android:exported="true" />
        <activity
            android:name=".EditStoreActivity"
            android:exported="false" />
        <activity
            android:name=".ViewRegisteredStoresActivity"
            android:exported="false" />
        <activity
            android:name=".About"
            android:exported="false" /> <!-- Exported set to true for activities accessible from outside the app -->
        <activity
            android:name=".RegisterStore"
            android:exported="true" /> <!-- Set this to true if it is accessed from other activities -->
        <activity
            android:name=".AlertCenter"
            android:exported="true" /> <!-- Set this to true if it is accessed from other activities -->
        <activity
            android:name=".LocatorMap"
            android:exported="true" /> <!-- Set this to true if it is accessed from other activities -->
        <activity
            android:name=".Dashboard"
            android:exported="true" /> <!-- Set this to true to allow launching this activity -->
        <activity
            android:name=".Signup"
            android:exported="false" /> <!-- Set this to true if it is accessed from other activities -->
        <activity
            android:name=".Signin"
            android:exported="false" /> <!-- Set this to true if it is accessed from other activities -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>